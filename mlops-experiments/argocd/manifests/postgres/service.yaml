apiVersion: v1
kind: Service
metadata:
  name: mlflow-postgres-postgresql
  namespace: application
  labels:
    app: mlflow-postgres
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: mlflow-postgres
