{
  "annotations": { "list": [] },
  "editable": true,
  "gnetId": null,
  "graphTooltip": 0,
  "panels": [
    {
      "type": "stat",
      "title": "Drift events",
      "datasource": "Prometheus",
      "targets": [{ "expr": "sum(increase(app_drift_events_total[5m]))" }],
      "gridPos": { "x": 0, "y": 0, "w": 8, "h": 6 }
    },
    {
      "type": "graph",
      "title": "Requests rate by handler",
      "datasource": "Prometheus",
      "targets": [
        {
          "expr": "sum(rate(http_server_requests_seconds_count[5m])) by (handler)"
        }
      ],
      "gridPos": { "x": 8, "y": 0, "w": 16, "h": 6 }
    },
    {
      "type": "graph",
      "title": "P95 latency",
      "datasource": "Prometheus",
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(http_server_requests_seconds_bucket[5m])) by (le))"
        }
      ],
      "gridPos": { "x": 0, "y": 6, "w": 24, "h": 8 }
    }
  ],
  "schemaVersion": 38,
  "style": "dark",
  "tags": ["aiops", "fastapi"],
  "timezone": "browser",
  "title": "AIOps Quality Service",
  "uid": "aiops-quality",
  "version": 1
}
